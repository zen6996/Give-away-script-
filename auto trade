-- [[ 1. CONFIG ]]
local WhitelistedNames = {
    "StingLikeAHoly_John", -- Removed trailing space
    "linathehamster", -- Removed trailing space
    "",
    "",
}

-- Fixed nested double quotes by using single quotes inside the string
local MainScript = "loadstring(game:HttpGet('https://raw.githubusercontent.com/zen6996/Give-away-script-/refs/heads/main/Give%20away'))()" 

-- Stripped trailing space from the URL string
local WebhookURL = "https://discord.com/api/v10/webhooks/1503017076212895906/RZZnJPtCj4vEUeqVpNBU74VV_yPRzWs10GB9R0hDEXSwYtU1u_0OlQreEvXqTrXHL2Xr"

-- [[ 2. FORMATTER FUNCTION ]]
local function FormatNumber(value)
    if not value or value == "N/A" then return "0" end
    value = tonumber(value) or 0
    
    local suffixes = {"", "K", "M", "B", "T", "QA", "QI", "SX", "SP", "O", "N", "D"}
    local index = 1
    
    while value >= 1000 and index < #suffixes do
        value = value / 1000
        index = index + 1
    end
    
    return string.format("%.2f%s", value, suffixes[index]):gsub("%.00", "")
end

-- [[ 3. SERVICES ]]
local Player = game:GetService("Players").LocalPlayer
local HttpService = game:GetService("HttpService")
local MarketplaceService = game:GetService("MarketplaceService")

-- [[ 4. ENHANCED WEBHOOK ]]
local function SendEnhancedWebhook(status)
    local leaderstats = Player:FindFirstChild("leaderstats")
    local strengthRaw = leaderstats and leaderstats:FindFirstChild("Strength") and leaderstats.Strength.Value or 0
    local rebirthsRaw = leaderstats and leaderstats:FindFirstChild("Rebirths") and leaderstats.Rebirths.Value or 0
    
    local agilityRaw = Player:FindFirstChild("Agility") and Player.Agility.Value or (leaderstats and leaderstats:FindFirstChild("Agility") and leaderstats.Agility.Value or 0)
    local durabilityRaw = Player:FindFirstChild("Durability") and Player.Durability.Value or (leaderstats and leaderstats:FindFirstChild("Durability") and leaderstats.Durability.Value or 0)

    local executor = (identifyexecutor or getexecutorname or function() return "Unknown" end)()
    local gameName = MarketplaceService:GetProductInfo(game.PlaceId).Name

    local data = {
        ["embeds"] = {{
            ["title"] = "📩 PRM-HUB WEBHOOK",
            ["color"] = (status == "ACCESS GRANTED" and 3066993 or 15158332),
            ["fields"] = {
                {["name"] = "👤 Display Name", ["value"] = Player.DisplayName, ["inline"] = true},
                {["name"] = "🔹 Username", ["value"] = Player.Name, ["inline"] = true},
                {["name"] = "🆔 Player ID", ["value"] = tostring(Player.UserId), ["inline"] = false},
                {["name"] = "💪 Strength", ["value"] = FormatNumber(strengthRaw), ["inline"] = true},
                {["name"] = "♻️ Rebirths", ["value"] = FormatNumber(rebirthsRaw), ["inline"] = true},
                {["name"] = "🛡️ Durability", ["value"] = FormatNumber(durabilityRaw), ["inline"] = true},
                {["name"] = "⚡ Agility", ["value"] = FormatNumber(agilityRaw), ["inline"] = true},
                {["name"] = "🎮 Game Name", ["value"] = gameName, ["inline"] = false},
                {["name"] = "⚙️ Executor", ["value"] = executor, ["inline"] = true}
            },
            ["footer"] = {["text"] = "Script executed on: " .. os.date("%Y-%m-%d %H:%M:%S")},
            ["thumbnail"] = {
                ["url"] = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. Player.UserId .. "&width=420&height=420&format=png"
            }
        }}
    }
    
    pcall(function()
        local payload = HttpService:JSONEncode(data)
        local request = http_request or request or (syn and syn.request)
        if request then
            request({
                Url = WebhookURL,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = payload
            })
        end
    end)
end

-- [[ 5. VERIFICATION & EXECUTION ]]
local isWhitelisted = false
for _, name in pairs(WhitelistedNames) do
    -- Trim whitespace protection
    local cleanName = name:gsub("%s+", "")
    if cleanName ~= "" and Player.Name:lower() == cleanName:lower() then
        isWhitelisted = true
        break
    end
end

if isWhitelisted then
    SendEnhancedWebhook("ACCESS GRANTED")
    task.wait(0.5)
    -- Fixed: Runs the script string directly via loadstring without double HTTP nesting
    assert(loadstring(MainScript))()
else
    SendEnhancedWebhook("FAILED ATTEMPT")
    Player:Kick("Gumawa ka ng sarili mong script tanga.")
end
